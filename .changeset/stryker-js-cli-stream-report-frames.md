---
"@systemfsoftware/stryker-js-cli": minor
---

the machine-mode stream frames the run stream's new plugin-load,
format-registry, and skip events; `STREAM_SCHEMA_VERSION` is now re-exported from
the language package, which owns the run stream's wire version
