{
  "devDependencies": {
    "@commitlint/cli": "catalog:",
    "@commitlint/config-conventional": "catalog:",
    "@commitlint/types": "catalog:",
    "@systemfsoftware/tsconfig": "catalog:",
    "@types/node": "catalog:",
    "husky": "catalog:",
    "lint-staged": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "@effect/tsgo": "catalog:",
    "concurrently": "catalog:"
  },
  "license": "Apache-2.0",
  "name": "stryker-js-effect",
  "packageManager": "pnpm@11.21.0",
  "private": true,
  "scripts": {
    "attw": "turbo --concurrency=${TURBO_CONCURRENCY:-50%} attw",
    "build": "turbo --concurrency=${TURBO_CONCURRENCY:-50%} build",
    "check:ci": "s=0; pnpm format:check || s=1; TURBO_CONCURRENCY=${TURBO_CONCURRENCY:-100%} pnpm gate:tasks || s=1; pnpm gate:dist || s=1; exit $s",
    "clean": "turbo clean",
    "format": "./bin/dprint fmt",
    "format:check": "./bin/dprint check",
    "gate:dist": "turbo --concurrency=${TURBO_CONCURRENCY:-100%} build ${TURBO_BASE:+--filter=...[$TURBO_BASE]}",
    "gate:tasks": "turbo --concurrency=${TURBO_CONCURRENCY:-50%} --continue lint lint:tsgo typecheck test ${TURBO_BASE:+--filter=...[$TURBO_BASE]}",
    "lint": "turbo --concurrency=${TURBO_CONCURRENCY:-50%} lint",
    "lgtm:down": "process-compose down",
    "lgtm:up": "process-compose up --tui=false --detached --keep-project && timeout 180 process-compose project is-ready --wait",
    "precommit": "pnpm exec lint-staged --no-revert --config .lintstagedrc.js",
    "prepare": "concurrently \"husky\" \"effect-tsgo patch --oxlint --no-typescript --skip-missing\"",
    "publish:unpublished": "./scripts/publish-and-setup-npm-trust.ts",
    "test": "turbo --concurrency=${TURBO_CONCURRENCY:-50%} test",
    "test:e2e": "turbo --concurrency=${TURBO_CONCURRENCY:-50%} test:e2e",
    "typecheck": "turbo --concurrency=${TURBO_CONCURRENCY:-50%} --continue typecheck typecheck:node",
    "typecheck:node": "tsc -p tsconfig.node.json --noEmit"
  },
  "type": "module"
}
